/* ********************************************************************************************
/* @name     : MAIN.C
/* @brief    : TEST PROGRAM OF THE QUEUE
/* @author   : TAREK ALLAM
/***********************************************************************************************/
/*************************************** START INCLUDES ****************************************/
#include "errorState.h"
#include "queue.h"
#include "stdTypes.h"

#include <stdio.h>

/*************************************** END INCLUDES ******************************************/
/*************************************** START MAIN ********************************************/
int main()
{
    // uint32 dataOUT;
    // queue_t myQueue;
    // q_enqueue(&myQueue, 1);
    // q_enqueue(&myQueue, 2);
    // q_enqueue(&myQueue, 3);
    // q_enqueue(&myQueue, 4);
    // q_enqueue(&myQueue, 5);
    // q_enqueue(&myQueue, 6);
    // q_enqueue(&myQueue, 7);
    // q_enqueue(&myQueue, 8);
    // q_enqueue(&myQueue, 9);
    // q_enqueue(&myQueue, 10);
    // q_showItems(&myQueue);
    // printf("------------------------ \n");
    // q_dequeue(&myQueue, &dataOUT);
    // q_dequeue(&myQueue, &dataOUT);
    // q_dequeue(&myQueue, &dataOUT);
    // q_dequeue(&myQueue, &dataOUT);
    // q_dequeue(&myQueue, &dataOUT);
    // q_dequeue(&myQueue, &dataOUT);
    // q_showItems(&myQueue);
    // printf("------------------------ \n");
    // q_enqueue(&myQueue, 10);
    // q_enqueue(&myQueue, 9);
    // q_enqueue(&myQueue, 8);
    // q_enqueue(&myQueue, 7);
    // q_enqueue(&myQueue, 6);
    // q_enqueue(&myQueue, 5);

    // q_showItems(&myQueue);

    return 0;
}
/*************************************** END MAIN **********************************************/

/************************************************************************************************
 * AUTHOR              DATE                    BRIEF
 ************************************************************************************************
 * TAREK ALLAM         31 oct 2022             TEST program
 *
 */